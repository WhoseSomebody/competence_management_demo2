/ %p#notice= notice
%h1 Відгуки користувачів
%table.feddb_table{:style => "width: 96%"}
  %tbody{:style => "text-align: left;"} 
    - feedbacks.each do |feedback|
      %tr
        %thІм’я:
        %td= feedback.name
        %td.creation_time_feedb{:rowspan => "3"}
          = l feedback.created_at, format: "%a %d %b %H:%M"
      %tr
        %th E-mail:
        %td= feedback.email
      %tr
        %th Зміст:
        %td= feedback.content
/        %td= link_to 'Видалити', feedback, method: :delete, data: { confirm: 'Ви впевнені?' }
%div{:style => "bottom: 0; margin: 0 auto; width: 55%; bottom: 40px;"}
  = paginate feedbacks